import {
    Button, IconButton, Badge, NumberInput, CheckBox,
    SwitchBox, AutoComplete, Tabs, DataTable, Divider, Indicator
} from "@/index";



export default function () {
    return(
        <div className="w-full h-full flex flex-col">

        </div>
    );
}